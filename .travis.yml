language: dart
dart:
  # Install the latest stable release
  - stable
branches:
  only:
    - master
script:
  - pub run test
  - pub run test/issues/issue9.dart
  - pub run test/issues/issue18.dart

